# Monochrome Unraid Configuration

# --- PORTS ---
# Port web de l'application (Défaut: 3001 pour éviter le conflit 3000)
MONOCHROME_PORT=3001

# Port de l'API PocketBase (Défaut: 8091 pour éviter les conflits)
POCKETBASE_PORT=8091

# Ports MinIO (S3)
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# Nom du bucket MinIO (créé automatiquement au premier démarrage)
MINIO_BUCKET_NAME=monochrome-music

# --- PATHS ---
# Chemin racine pour les données sur l'hôte (Unraid)
# Pour Unraid, cela mappe généralement vers /mnt/user/appdata/monochrome
HOST_DATA_PATH=./data

# --- URLs ---
# URL publique de PocketBase (accessible depuis le navigateur)
# Exemple: https://api.monochrome.mondomaine.com ou http://192.168.1.X:8090
PUBLIC_POCKETBASE_URL=https://pb.vonrodbox.eu
URL_BASE_PUBLIC=https://monochrome.tf
# Auth gate (optionnel)

# --- AUTHENTICATION ---
# PocketBase Admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_this_password

# MinIO Credentials
MINIO_ROOT_USER=monochrome
MINIO_ROOT_PASSWORD=change_this_secret_key

# --- SYSTEM ---
# Taille limite d'upload (0 = illimité)
BODY_SIZE_LIMIT=0
